;; Generated by clj -M:ir from the policy file. Do not edit.
{:title "SkillBoard",
 :diagrams
 [{:title "Layers",
   :direction :tb,
   :packages
   [{:id :main,
     :label "Main",
     :classes [{:id :core, :name "Core", :ns "skillBoard.core", :hide-members true}]}
    {:id :adapters,
     :label "Adapters",
     :classes
     [{:id :split-flap,
       :name "SplitFlap",
       :ns "skillBoard.split-flap",
       :hide-members true}
      {:id :heartbeat, :name "Heartbeat", :ns "skillBoard.heartbeat", :hide-members true}
      {:id :text-util,
       :name "TextUtil",
       :ns "skillBoard.text-util",
       :hide-members true}]}
    {:id :presenters,
     :label "Presenters",
     :classes
     [{:id :presenters.main,
       :name "PresentersMain",
       :ns "skillBoard.presenters.main",
       :hide-members true}
      {:id :presenters.flights,
       :name "PresentersFlights",
       :ns "skillBoard.presenters.flights",
       :hide-members true}
      {:id :presenters.traffic,
       :name "PresentersTraffic",
       :ns "skillBoard.presenters.traffic",
       :hide-members true}
      {:id :presenters.weather,
       :name "PresentersWeather",
       :ns "skillBoard.presenters.weather",
       :hide-members true}
      {:id :presenters.airports,
       :name "PresentersAirports",
       :ns "skillBoard.presenters.airports",
       :hide-members true}
      {:id :presenters.wind-map,
       :name "PresentersWindMap",
       :ns "skillBoard.presenters.wind-map",
       :hide-members true}
      {:id :presenters.wind-map.layer,
       :name "PresentersWindMapLayer",
       :ns "skillBoard.presenters.wind-map.layer",
       :hide-members true}
      {:id :presenters.wind-map.particles,
       :name "PresentersWindMapParticles",
       :ns "skillBoard.presenters.wind-map.particles",
       :hide-members true}
      {:id :presenters.wind-map.overlays,
       :name "PresentersWindMapOverlays",
       :ns "skillBoard.presenters.wind-map.overlays",
       :hide-members true}
      {:id :presenters.wind-map.markers,
       :name "PresentersWindMapMarkers",
       :ns "skillBoard.presenters.wind-map.markers",
       :hide-members true}
      {:id :presenters.wind-map.geo,
       :name "PresentersWindMapGeo",
       :ns "skillBoard.presenters.wind-map.geo",
       :hide-members true}
      {:id :presenters.wind-map.draw,
       :name "PresentersWindMapDraw",
       :ns "skillBoard.presenters.wind-map.draw",
       :hide-members true}
      {:id :presenters.utils,
       :name "PresentersUtils",
       :ns "skillBoard.presenters.utils",
       :hide-members true}
      {:id :presenters.screen,
       :name "PresentersScreen",
       :ns "skillBoard.presenters.screen",
       :hide-members true}]}
    {:id :gateways,
     :label "Gateways",
     :classes
     [{:id :wind-data, :name "WindData", :ns "skillBoard.wind-data", :hide-members true}
      {:id :comm-utils,
       :name "CommUtils",
       :ns "skillBoard.comm-utils",
       :hide-members true}]}
    {:id :domain,
     :label "Domain",
     :classes
     [{:id :flight-schedule-pro,
       :name "FlightSchedulePro",
       :ns "skillBoard.flight-schedule-pro",
       :hide-members true}
      {:id :radar-cape,
       :name "RadarCape",
       :ns "skillBoard.radar-cape",
       :hide-members true}
      {:id :navigation,
       :name "Navigation",
       :ns "skillBoard.navigation",
       :hide-members true}]}
    {:id :foundation,
     :label "Foundation",
     :classes
     [{:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}
      {:id :time-util, :name "TimeUtil", :ns "skillBoard.time-util", :hide-members true}
      {:id :atoms, :name "Atoms", :ns "skillBoard.atoms", :hide-members true}
      {:id :config, :name "Config", :ns "skillBoard.config", :hide-members true}]}
    {:id :unassigned,
     :label "Unassigned",
     :classes
     [{:id :acceptance-generator,
       :name "AcceptanceGenerator",
       :ns "skillBoard.acceptance-generator",
       :hide-members true}
      {:id :acceptance-test-runner,
       :name "AcceptanceTestRunner",
       :ns "skillBoard.acceptance-test-runner",
       :hide-members true}
      {:id :gherkin, :name "Gherkin", :ns "skillBoard.gherkin", :hide-members true}
      {:id :test-runner,
       :name "TestRunner",
       :ns "skillBoard.test-runner",
       :hide-members true}]}],
   :edges
   [{:from :acceptance-generator, :to :gherkin, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.screen, :kind :dependency}
    {:from :presenters.wind-map.particles,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.wind-map.layer, :to :presenters.wind-map.geo, :kind :dependency}
    {:from :comm-utils, :to :config, :kind :dependency}
    {:from :core, :to :wind-data, :kind :dependency}
    {:from :presenters.airports, :to :presenters.utils, :kind :dependency}
    {:from :presenters.main, :to :atoms, :kind :dependency}
    {:from :comm-utils, :to :core-utils, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :presenters.wind-map.geo, :kind :dependency}
    {:from :presenters.flights, :to :flight-schedule-pro, :kind :dependency}
    {:from :presenters.traffic, :to :presenters.utils, :kind :dependency}
    {:from :presenters.wind-map.markers,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.wind-map.draw, :to :config, :kind :dependency}
    {:from :flight-schedule-pro, :to :time-util, :kind :dependency}
    {:from :split-flap, :to :quil, :kind :dependency}
    {:from :split-flap, :to :presenters.screen, :kind :dependency}
    {:from :presenters.wind-map.overlays,
     :to :presenters.wind-map.geo,
     :kind :dependency}
    {:from :presenters.flights, :to :radar-cape, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :config, :kind :dependency}
    {:from :wind-data, :to :config, :kind :dependency}
    {:from :split-flap, :to :time-util, :kind :dependency}
    {:from :heartbeat, :to :atoms, :kind :dependency}
    {:from :heartbeat, :to :core-utils, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :quil, :kind :dependency}
    {:from :presenters.screen, :to :config, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :config, :kind :dependency}
    {:from :presenters.flights, :to :config, :kind :dependency}
    {:from :wind-data, :to :comm-utils, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :quil, :kind :dependency}
    {:from :core, :to :quil, :kind :dependency}
    {:from :core, :to :presenters.weather, :kind :dependency}
    {:from :presenters.wind-map.layer, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :quil, :kind :dependency}
    {:from :comm-utils, :to :java-time, :kind :dependency}
    {:from :presenters.wind-map.layer,
     :to :presenters.wind-map.markers,
     :kind :dependency}
    {:from :presenters.airports, :to :config, :kind :dependency}
    {:from :presenters.traffic, :to :atoms, :kind :dependency}
    {:from :core, :to :core-utils, :kind :dependency}
    {:from :presenters.main, :to :config, :kind :dependency}
    {:from :core, :to :comm-utils, :kind :dependency}
    {:from :core-utils, :to :config, :kind :dependency}
    {:from :split-flap, :to :config, :kind :dependency}
    {:from :presenters.wind-map.layer,
     :to :presenters.wind-map.overlays,
     :kind :dependency}
    {:from :test-runner, :to :acceptance-generator, :kind :dependency}
    {:from :presenters.main, :to :presenters.screen, :kind :dependency}
    {:from :presenters.traffic, :to :quil, :kind :dependency}
    {:from :presenters.weather, :to :comm-utils, :kind :dependency}
    {:from :presenters.utils, :to :config, :kind :dependency}
    {:from :presenters.traffic, :to :core-utils, :kind :dependency}
    {:from :presenters.traffic, :to :comm-utils, :kind :dependency}
    {:from :split-flap, :to :comm-utils, :kind :dependency}
    {:from :presenters.flights, :to :comm-utils, :kind :dependency}
    {:from :core, :to :atoms, :kind :dependency}
    {:from :core-utils, :to :time-util, :kind :dependency}
    {:from :presenters.weather, :to :config, :kind :dependency}
    {:from :comm-utils, :to :clojure.data.json, :kind :dependency}
    {:from :text-util, :to :quil, :kind :dependency}
    {:from :presenters.traffic, :to :presenters.screen, :kind :dependency}
    {:from :core, :to :presenters.traffic, :kind :dependency}
    {:from :heartbeat, :to :clojure.data.json, :kind :dependency}
    {:from :flight-schedule-pro, :to :java-time, :kind :dependency}
    {:from :presenters.utils, :to :comm-utils, :kind :dependency}
    {:from :acceptance-generator, :to :clojure.data.json, :kind :dependency}
    {:from :core, :to :heartbeat, :kind :dependency}
    {:from :core, :to :split-flap, :kind :dependency}
    {:from :wind-data, :to :clj-http, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.geo, :kind :dependency}
    {:from :presenters.airports, :to :comm-utils, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :config, :kind :dependency}
    {:from :core, :to :text-util, :kind :dependency}
    {:from :split-flap, :to :presenters.main, :kind :dependency}
    {:from :presenters.wind-map, :to :config, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.layer, :kind :dependency}
    {:from :acceptance-test-runner, :to :test-runner, :kind :dependency}
    {:from :core, :to :presenters.flights, :kind :dependency}
    {:from :presenters.weather, :to :presenters.utils, :kind :dependency}
    {:from :presenters.wind-map.draw, :to :quil, :kind :dependency}
    {:from :wind-data, :to :core-utils, :kind :dependency}
    {:from :presenters.screen, :to :quil, :kind :dependency}
    {:from :heartbeat, :to :clj-http, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :presenters.utils, :kind :dependency}
    {:from :presenters.flights, :to :presenters.utils, :kind :dependency}
    {:from :flight-schedule-pro, :to :core-utils, :kind :dependency}
    {:from :core, :to :config, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :presenters.wind-map.draw, :kind :dependency}
    {:from :heartbeat, :to :config, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :clojure.data.json, :kind :dependency}
    {:from :time-util, :to :config, :kind :dependency}
    {:from :presenters.flights, :to :time-util, :kind :dependency}
    {:from :presenters.wind-map.particles,
     :to :presenters.wind-map.geo,
     :kind :dependency}
    {:from :presenters.traffic, :to :config, :kind :dependency}
    {:from :presenters.wind-map, :to :wind-data, :kind :dependency}
    {:from :presenters.wind-map, :to :quil, :kind :dependency}
    {:from :split-flap, :to :java-time, :kind :dependency}
    {:from :presenters.flights, :to :navigation, :kind :dependency}
    {:from :wind-data, :to :java-time, :kind :dependency}
    {:from :core, :to :presenters.airports, :kind :dependency}
    {:from :gherkin, :to :clojure.data.json, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :wind-data, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.overlays, :kind :dependency}
    {:from :split-flap, :to :atoms, :kind :dependency}
    {:from :presenters.utils, :to :navigation, :kind :dependency}
    {:from :presenters.traffic, :to :navigation, :kind :dependency}
    {:from :core, :to :presenters.main, :kind :dependency}
    {:from :presenters.main, :to :presenters.utils, :kind :dependency}
    {:from :radar-cape, :to :java-time, :kind :dependency}
    {:from :presenters.weather, :to :presenters.screen, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.markers, :kind :dependency}
    {:from :wind-data, :to :clojure.data.json, :kind :dependency}
    {:from :presenters.airports, :to :presenters.screen, :kind :dependency}
    {:from :comm-utils, :to :clj-http, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :core-utils, :kind :dependency}
    {:from :presenters.flights, :to :presenters.screen, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.particles, :kind :dependency}
    {:from :presenters.airports, :to :navigation, :kind :dependency}
    {:from :core-utils, :to :atoms, :kind :dependency}
    {:from :presenters.wind-map.overlays,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.flights, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :comm-utils, :kind :dependency}
    {:from :core-utils, :to :java-time, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :wind-data, :kind :dependency}
    {:from :core, :to :presenters.wind-map, :kind :dependency}
    {:from :presenters.flights, :to :atoms, :kind :dependency}
    {:from :presenters.wind-map.layer, :to :atoms, :kind :dependency}
    {:from :time-util, :to :java-time, :kind :dependency}],
   :foreign
   [{:id :clj-http, :name "clj-http", :shape :oval}
    {:id :clojure.data.json, :name "clojure.data.json", :shape :oval}
    {:id :java-time, :name "java-time", :shape :oval}
    {:id :quil, :name "quil", :shape :oval}]}
  {:title "Main",
   :direction :lr,
   :packages
   [{:id :main,
     :label "Main",
     :classes
     [{:id :core, :name "Core", :ns "skillBoard.core"}
      {:id :wind-data, :name "WindData", :ns "skillBoard.wind-data", :hide-members true}
      {:id :presenters.weather,
       :name "PresentersWeather",
       :ns "skillBoard.presenters.weather",
       :hide-members true}
      {:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}
      {:id :comm-utils,
       :name "CommUtils",
       :ns "skillBoard.comm-utils",
       :hide-members true}
      {:id :atoms, :name "Atoms", :ns "skillBoard.atoms", :hide-members true}
      {:id :presenters.traffic,
       :name "PresentersTraffic",
       :ns "skillBoard.presenters.traffic",
       :hide-members true}
      {:id :heartbeat, :name "Heartbeat", :ns "skillBoard.heartbeat", :hide-members true}
      {:id :split-flap,
       :name "SplitFlap",
       :ns "skillBoard.split-flap",
       :hide-members true}
      {:id :text-util, :name "TextUtil", :ns "skillBoard.text-util", :hide-members true}
      {:id :presenters.flights,
       :name "PresentersFlights",
       :ns "skillBoard.presenters.flights",
       :hide-members true}
      {:id :config, :name "Config", :ns "skillBoard.config", :hide-members true}
      {:id :presenters.airports,
       :name "PresentersAirports",
       :ns "skillBoard.presenters.airports",
       :hide-members true}
      {:id :presenters.main,
       :name "PresentersMain",
       :ns "skillBoard.presenters.main",
       :hide-members true}
      {:id :presenters.wind-map,
       :name "PresentersWindMap",
       :ns "skillBoard.presenters.wind-map",
       :hide-members true}]}],
   :edges
   [{:from :core, :to :wind-data, :kind :association}
    {:from :core, :to :quil, :kind :dependency}
    {:from :core, :to :presenters.weather, :kind :dependency}
    {:from :core, :to :core-utils, :kind :dependency}
    {:from :core, :to :comm-utils, :kind :association}
    {:from :core, :to :atoms, :kind :dependency}
    {:from :core, :to :presenters.traffic, :kind :dependency}
    {:from :core, :to :heartbeat, :kind :association}
    {:from :core, :to :split-flap, :kind :association}
    {:from :core, :to :text-util, :kind :association}
    {:from :core, :to :presenters.flights, :kind :dependency}
    {:from :core, :to :config, :kind :dependency}
    {:from :core, :to :presenters.airports, :kind :dependency}
    {:from :core, :to :presenters.main, :kind :association}
    {:from :core, :to :presenters.wind-map, :kind :dependency}],
   :foreign [{:id :quil, :name "quil", :shape :oval}]}
  {:title "Adapters",
   :direction :lr,
   :packages
   [{:id :adapters,
     :label "Adapters",
     :classes
     [{:id :split-flap, :name "SplitFlap", :ns "skillBoard.split-flap"}
      {:id :heartbeat, :name "Heartbeat", :ns "skillBoard.heartbeat"}
      {:id :text-util, :name "TextUtil", :ns "skillBoard.text-util"}
      {:id :presenters.screen,
       :name "PresentersScreen",
       :ns "skillBoard.presenters.screen",
       :hide-members true}
      {:id :time-util, :name "TimeUtil", :ns "skillBoard.time-util", :hide-members true}
      {:id :atoms, :name "Atoms", :ns "skillBoard.atoms", :hide-members true}
      {:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}
      {:id :config, :name "Config", :ns "skillBoard.config", :hide-members true}
      {:id :comm-utils,
       :name "CommUtils",
       :ns "skillBoard.comm-utils",
       :hide-members true}
      {:id :presenters.main,
       :name "PresentersMain",
       :ns "skillBoard.presenters.main",
       :hide-members true}]}],
   :edges
   [{:from :split-flap, :to :quil, :kind :dependency}
    {:from :split-flap, :to :presenters.screen, :kind :association}
    {:from :split-flap, :to :time-util, :kind :dependency}
    {:from :heartbeat, :to :atoms, :kind :dependency}
    {:from :heartbeat, :to :core-utils, :kind :association}
    {:from :split-flap, :to :config, :kind :dependency}
    {:from :split-flap, :to :comm-utils, :kind :dependency}
    {:from :text-util, :to :quil, :kind :dependency}
    {:from :heartbeat, :to :clojure.data.json, :kind :dependency}
    {:from :split-flap, :to :presenters.main, :kind :association}
    {:from :heartbeat, :to :clj-http, :kind :dependency}
    {:from :heartbeat, :to :config, :kind :dependency}
    {:from :split-flap, :to :java-time, :kind :dependency}
    {:from :split-flap, :to :atoms, :kind :dependency}],
   :foreign
   [{:id :clj-http, :name "clj-http", :shape :oval}
    {:id :clojure.data.json, :name "clojure.data.json", :shape :oval}
    {:id :java-time, :name "java-time", :shape :oval}
    {:id :quil, :name "quil", :shape :oval}]}
  {:title "Presenters",
   :direction :lr,
   :packages
   [{:id :presenters,
     :label "Presenters",
     :classes
     [{:id :presenters.main, :name "PresentersMain", :ns "skillBoard.presenters.main"}
      {:id :presenters.flights,
       :name "PresentersFlights",
       :ns "skillBoard.presenters.flights"}
      {:id :presenters.traffic,
       :name "PresentersTraffic",
       :ns "skillBoard.presenters.traffic"}
      {:id :presenters.weather,
       :name "PresentersWeather",
       :ns "skillBoard.presenters.weather"}
      {:id :presenters.airports,
       :name "PresentersAirports",
       :ns "skillBoard.presenters.airports"}
      {:id :presenters.wind-map,
       :name "PresentersWindMap",
       :ns "skillBoard.presenters.wind-map"}
      {:id :presenters.wind-map.layer,
       :name "PresentersWindMapLayer",
       :ns "skillBoard.presenters.wind-map.layer"}
      {:id :presenters.wind-map.particles,
       :name "PresentersWindMapParticles",
       :ns "skillBoard.presenters.wind-map.particles"}
      {:id :presenters.wind-map.overlays,
       :name "PresentersWindMapOverlays",
       :ns "skillBoard.presenters.wind-map.overlays"}
      {:id :presenters.wind-map.markers,
       :name "PresentersWindMapMarkers",
       :ns "skillBoard.presenters.wind-map.markers"}
      {:id :presenters.wind-map.geo,
       :name "PresentersWindMapGeo",
       :ns "skillBoard.presenters.wind-map.geo"}
      {:id :presenters.wind-map.draw,
       :name "PresentersWindMapDraw",
       :ns "skillBoard.presenters.wind-map.draw"}
      {:id :presenters.utils, :name "PresentersUtils", :ns "skillBoard.presenters.utils"}
      {:id :presenters.screen,
       :name "PresentersScreen",
       :ns "skillBoard.presenters.screen"}
      {:id :atoms, :name "Atoms", :ns "skillBoard.atoms", :hide-members true}
      {:id :flight-schedule-pro,
       :name "FlightSchedulePro",
       :ns "skillBoard.flight-schedule-pro",
       :hide-members true}
      {:id :config, :name "Config", :ns "skillBoard.config", :hide-members true}
      {:id :radar-cape,
       :name "RadarCape",
       :ns "skillBoard.radar-cape",
       :hide-members true}
      {:id :comm-utils,
       :name "CommUtils",
       :ns "skillBoard.comm-utils",
       :hide-members true}
      {:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}
      {:id :time-util, :name "TimeUtil", :ns "skillBoard.time-util", :hide-members true}
      {:id :wind-data, :name "WindData", :ns "skillBoard.wind-data", :hide-members true}
      {:id :navigation,
       :name "Navigation",
       :ns "skillBoard.navigation",
       :hide-members true}]}],
   :edges
   [{:from :presenters.wind-map, :to :presenters.screen, :kind :association}
    {:from :presenters.wind-map.particles,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.wind-map.layer, :to :presenters.wind-map.geo, :kind :association}
    {:from :presenters.airports, :to :presenters.utils, :kind :dependency}
    {:from :presenters.main, :to :atoms, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :presenters.wind-map.geo, :kind :dependency}
    {:from :presenters.flights, :to :flight-schedule-pro, :kind :dependency}
    {:from :presenters.traffic, :to :presenters.utils, :kind :dependency}
    {:from :presenters.wind-map.markers,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.wind-map.draw, :to :config, :kind :dependency}
    {:from :presenters.wind-map.overlays,
     :to :presenters.wind-map.geo,
     :kind :dependency}
    {:from :presenters.flights, :to :radar-cape, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :config, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :quil, :kind :dependency}
    {:from :presenters.screen, :to :config, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :config, :kind :dependency}
    {:from :presenters.flights, :to :config, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.layer, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.layer,
     :to :presenters.wind-map.markers,
     :kind :association}
    {:from :presenters.airports, :to :config, :kind :dependency}
    {:from :presenters.traffic, :to :atoms, :kind :dependency}
    {:from :presenters.main, :to :config, :kind :dependency}
    {:from :presenters.wind-map.layer,
     :to :presenters.wind-map.overlays,
     :kind :association}
    {:from :presenters.main, :to :presenters.screen, :kind :association}
    {:from :presenters.traffic, :to :quil, :kind :dependency}
    {:from :presenters.weather, :to :comm-utils, :kind :dependency}
    {:from :presenters.utils, :to :config, :kind :dependency}
    {:from :presenters.traffic, :to :core-utils, :kind :dependency}
    {:from :presenters.traffic, :to :comm-utils, :kind :dependency}
    {:from :presenters.flights, :to :comm-utils, :kind :dependency}
    {:from :presenters.weather, :to :config, :kind :dependency}
    {:from :presenters.traffic, :to :presenters.screen, :kind :association}
    {:from :presenters.utils, :to :comm-utils, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.geo, :kind :dependency}
    {:from :presenters.airports, :to :comm-utils, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :config, :kind :dependency}
    {:from :presenters.wind-map, :to :config, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.layer, :kind :association}
    {:from :presenters.weather, :to :presenters.utils, :kind :dependency}
    {:from :presenters.wind-map.draw, :to :quil, :kind :dependency}
    {:from :presenters.screen, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :presenters.utils, :kind :dependency}
    {:from :presenters.flights, :to :presenters.utils, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :presenters.wind-map.draw, :kind :dependency}
    {:from :presenters.wind-map.geo, :to :clojure.data.json, :kind :dependency}
    {:from :presenters.flights, :to :time-util, :kind :dependency}
    {:from :presenters.wind-map.particles,
     :to :presenters.wind-map.geo,
     :kind :association}
    {:from :presenters.traffic, :to :config, :kind :dependency}
    {:from :presenters.wind-map, :to :wind-data, :kind :dependency}
    {:from :presenters.wind-map, :to :quil, :kind :dependency}
    {:from :presenters.flights, :to :navigation, :kind :dependency}
    {:from :presenters.wind-map.overlays, :to :wind-data, :kind :dependency}
    {:from :presenters.wind-map, :to :presenters.wind-map.overlays, :kind :dependency}
    {:from :presenters.utils, :to :navigation, :kind :dependency}
    {:from :presenters.traffic, :to :navigation, :kind :dependency}
    {:from :presenters.main, :to :presenters.utils, :kind :association}
    {:from :presenters.weather, :to :presenters.screen, :kind :association}
    {:from :presenters.wind-map, :to :presenters.wind-map.markers, :kind :dependency}
    {:from :presenters.airports, :to :presenters.screen, :kind :association}
    {:from :presenters.wind-map.overlays, :to :core-utils, :kind :dependency}
    {:from :presenters.flights, :to :presenters.screen, :kind :association}
    {:from :presenters.wind-map, :to :presenters.wind-map.particles, :kind :association}
    {:from :presenters.airports, :to :navigation, :kind :dependency}
    {:from :presenters.wind-map.overlays,
     :to :presenters.wind-map.draw,
     :kind :dependency}
    {:from :presenters.flights, :to :quil, :kind :dependency}
    {:from :presenters.wind-map.markers, :to :comm-utils, :kind :dependency}
    {:from :presenters.wind-map.particles, :to :wind-data, :kind :dependency}
    {:from :presenters.flights, :to :atoms, :kind :dependency}
    {:from :presenters.wind-map.layer, :to :atoms, :kind :dependency}],
   :foreign
   [{:id :clojure.data.json, :name "clojure.data.json", :shape :oval}
    {:id :quil, :name "quil", :shape :oval}]}
  {:title "Gateways",
   :direction :lr,
   :packages
   [{:id :gateways,
     :label "Gateways",
     :classes
     [{:id :wind-data, :name "WindData", :ns "skillBoard.wind-data"}
      {:id :comm-utils, :name "CommUtils", :ns "skillBoard.comm-utils"}
      {:id :config, :name "Config", :ns "skillBoard.config", :hide-members true}
      {:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}]}],
   :edges
   [{:from :comm-utils, :to :config, :kind :dependency}
    {:from :comm-utils, :to :core-utils, :kind :dependency}
    {:from :wind-data, :to :config, :kind :dependency}
    {:from :wind-data, :to :comm-utils, :kind :dependency}
    {:from :comm-utils, :to :java-time, :kind :dependency}
    {:from :comm-utils, :to :clojure.data.json, :kind :dependency}
    {:from :wind-data, :to :clj-http, :kind :dependency}
    {:from :wind-data, :to :core-utils, :kind :dependency}
    {:from :wind-data, :to :java-time, :kind :dependency}
    {:from :wind-data, :to :clojure.data.json, :kind :dependency}
    {:from :comm-utils, :to :clj-http, :kind :dependency}],
   :foreign
   [{:id :clj-http, :name "clj-http", :shape :oval}
    {:id :clojure.data.json, :name "clojure.data.json", :shape :oval}
    {:id :java-time, :name "java-time", :shape :oval}]}
  {:title "Domain",
   :direction :lr,
   :packages
   [{:id :domain,
     :label "Domain",
     :classes
     [{:id :flight-schedule-pro,
       :name "FlightSchedulePro",
       :ns "skillBoard.flight-schedule-pro"}
      {:id :radar-cape, :name "RadarCape", :ns "skillBoard.radar-cape"}
      {:id :navigation, :name "Navigation", :ns "skillBoard.navigation"}
      {:id :time-util, :name "TimeUtil", :ns "skillBoard.time-util", :hide-members true}
      {:id :core-utils,
       :name "CoreUtils",
       :ns "skillBoard.core-utils",
       :hide-members true}]}],
   :edges
   [{:from :flight-schedule-pro, :to :time-util, :kind :dependency}
    {:from :flight-schedule-pro, :to :java-time, :kind :dependency}
    {:from :flight-schedule-pro, :to :core-utils, :kind :dependency}
    {:from :radar-cape, :to :java-time, :kind :dependency}],
   :foreign [{:id :java-time, :name "java-time", :shape :oval}]}
  {:title "Foundation",
   :direction :lr,
   :packages
   [{:id :foundation,
     :label "Foundation",
     :classes
     [{:id :core-utils, :name "CoreUtils", :ns "skillBoard.core-utils"}
      {:id :time-util, :name "TimeUtil", :ns "skillBoard.time-util"}
      {:id :atoms, :name "Atoms", :ns "skillBoard.atoms"}
      {:id :config, :name "Config", :ns "skillBoard.config"}]}],
   :edges
   [{:from :core-utils, :to :config, :kind :association}
    {:from :core-utils, :to :time-util, :kind :association}
    {:from :time-util, :to :config, :kind :association}
    {:from :core-utils, :to :atoms, :kind :association}
    {:from :core-utils, :to :java-time, :kind :dependency}
    {:from :time-util, :to :java-time, :kind :dependency}],
   :foreign [{:id :java-time, :name "java-time", :shape :oval}]}]}
